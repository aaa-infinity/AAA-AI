<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- By default, cleartext (HTTP) traffic is blocked. All app traffic must use HTTPS. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
